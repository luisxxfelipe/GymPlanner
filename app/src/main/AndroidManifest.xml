<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_fitness"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_fitness"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymPlanner"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.treinos.TreinosActivity" />
        <activity android:name=".ui.treinos.TreinoFormActivity" />
        <activity android:name=".ui.exercicios.ExercicioFormActivity" />
        <activity android:name=".ui.exercicios.ExerciciosActivity" />
        <activity android:name=".ui.registro.RegistroActivity" />
    </application>

</manifest>